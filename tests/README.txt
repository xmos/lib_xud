
The xross/feat/usb_shim branches need pulling into the mainlines of the
folliwing repos

xsim 
xsimutils
tools_xmostest

Run in pytest:
pip install pytest

To run all tests, simply type:
pytest

To run specifiC test, type:
pytest test_xx_xx.py
